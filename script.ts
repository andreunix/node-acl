// import { PrismaClient } from "@prisma/client";

// const prisma = new PrismaClient();


// prisma.product.findMany().then(code => console.log(code))

const x = ['x','x','x','x','x','x','x','x']

let rol: string[]

for (let id of x) {
  rol.push(id)
}

console.log(rol)